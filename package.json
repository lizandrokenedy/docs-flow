{
  "name": "docs-flow",
  "private": true,
  "packageManager": "npm@11.13.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "docker:dev": "bash scripts/build.sh dev",
    "docker:prod": "bash scripts/build.sh prod",
    "docker:down": "bash scripts/build.sh down",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:migrate": "docker compose -f docker-compose.dev.yml run --rm migrate",
    "db:seed": "docker compose -f docker-compose.dev.yml run --rm seed",
    "db:generate": "docker compose -f docker-compose.dev.yml run --rm migrate sh -c \"npx prisma generate --schema=prisma/schema.prisma\"",
    "db:reset": "docker compose -f docker-compose.dev.yml down -v && docker compose -f docker-compose.dev.yml up -d postgres && sleep 3 && npm run db:migrate && npm run db:seed"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
