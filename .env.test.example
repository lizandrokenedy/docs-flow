# Ambiente isolado para docker-compose.test.yml (não use no dev/prod).
# Uso: cp .env.test.example .env.test

DATABASE_URL=postgresql://docsflow:docsflow@postgres-test:5432/docsflow_test?schema=public

PORT=4000
UPLOAD_DIR=/app/uploads

CLAMAV_ENABLED=false

WEB_URL=http://localhost:3000
ADMIN_URL=http://localhost:3001
